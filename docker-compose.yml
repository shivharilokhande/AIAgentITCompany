services:
  aiagentitcompany:
    build: .
    image: aiagentitcompany:latest
    container_name: aiagentitcompany
    ports:
      - "3100:3000"
    environment:
      - DATA_DIR=/data
      - SEED_DEMO=1
    volumes:
      - smartit-data:/data
    restart: unless-stopped
volumes:
  smartit-data:
